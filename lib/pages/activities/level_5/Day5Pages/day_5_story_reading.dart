import 'package:flutter/material.dart';

class DayFiveStoryPage extends StatelessWidget {
  const DayFiveStoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Day 4 Story Reading Page'));
  }
}
