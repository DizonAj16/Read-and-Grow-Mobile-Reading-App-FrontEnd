import 'package:flutter/material.dart';

class PenguinMultipleChoicePage extends StatelessWidget {
  const PenguinMultipleChoicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Penguin Multiple Choice')),
      body: Center(child: Text('Penguin Multiple Choice Page')),
    );
  }
}
